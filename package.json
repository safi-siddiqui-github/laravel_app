{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "push": "git add . && git commit -m wip && git push",
        "deploy": "flyctl deploy",
        "gitploy": "git add . && git commit -m wip && git push && flyctl deploy",
        "migrate": "php artisan migrate:fresh --seed"
    },
    "devDependencies": {
        "@vitejs/plugin-vue": "^5.0.3",
        "autoprefixer": "^10.4.16",
        "axios": "^1.6.1",
        "laravel-vite-plugin": "^1.0.0",
        "postcss": "^8.4.33",
        "tailwindcss": "^3.4.1",
        "vite": "^5.0.0"
    },
    "dependencies": {
        "@inertiajs/vue3": "^1.0.14"
    }
}
